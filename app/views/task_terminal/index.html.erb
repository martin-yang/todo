<!DOCTYPE html>
<html>
<head>
  <title>任务管理</title>
  <link rel="stylesheet" href="/stylesheets/bootstrap.css">
  <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/font-awesome.css">
</head>
<body>
<div class="row">
  <nav style="margin-bottom: 0" role="navigation" class="navbar navbar-default navbar-fixed-top">
    <div class="navbar-header">
      <a ui-sref="home" class="navbar-brand"><%= I18n.t "email.title",today: @today_tasks[:date] %></a>
    </div>
  </nav>

  <div style="padding-top: 60px;">
    <div class="col-lg-6">
    <h3>任务清单</h3>
    </div>
    <div class="col-lg-6">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>任务</th>
            <th style="text-align: left;">状态</th>
          </tr>
        </thead>
        <tbody>
          <% @today_tasks[:tasks].each do |task| %>
            <tr>
              <td style='width: 200px;'><%= task.content %></td>
              <td style="text-align: left;">
              	<% if task.state.eql?("new") %>
                  <a class="btn btn-danger"><%= I18n.t "task.state.#{task.state}" %></a>
                <% else %>
                  <a class="btn btn-success"><%= I18n.t "task.state.#{task.state}" %></a>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
</body>
</html>
